/** Edge-safe constants (no Node built-ins). Import these from middleware. */
export const SESSION_COOKIE = "alt_session";
export const CSRF_COOKIE = "alt_csrf";
export const CSRF_HEADER = "x-csrf-token";
